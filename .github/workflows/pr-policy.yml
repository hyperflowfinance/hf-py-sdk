name: PR Policy

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read
  statuses: write

jobs:
  pr-policy:
    name: PR Policy
    uses: hyperflowfinance/.github/.github/workflows/policy.yml@main
    permissions:
      contents: read
      pull-requests: read
      statuses: write
